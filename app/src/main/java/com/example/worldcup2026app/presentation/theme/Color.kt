package com.example.worldcup2026app.presentation.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores oscuros (Dark Theme)
val DarkBackground = Color(0xFF0D1117) // Casi negro, excelente para contrastar imágenes
val DarkSurface = Color(0xFF161C22)    // Gris oscuro para las tarjetas
val DarkSurfaceVariant = Color(0xFF21262D) // Un tono un poco más claro para bordes

// Colores de Acento (Vibrantes)
val PrimaryAccent = Color(0xFF00FF87) // Verde neón deportivo
val SecondaryAccent = Color(0xFF00E5FF) // Cian vibrante

// Textos
val TextPrimary = Color(0xFFFFFFFF)    // Blanco puro para títulos
val TextSecondary = Color(0xFF8B949E)  // Gris claro para subtítulos (ej. posición del jugador)
val ErrorColor = Color(0xFFFF5555)     // Rojo vibrante para mensajes de error sin conexión